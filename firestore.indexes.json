{
  "//": [
    "게시판 목록은 '말머리 필터 + 정렬 + 개수 제한' 을 서버에서 처리한다.",
    "Firestore 는 where 와 orderBy 를 서로 다른 필드에 함께 쓰면 복합 색인을 요구하므로,",
    "아래 두 가지가 필요하다. (필터 없이 정렬만 하는 경우는 자동 색인으로 처리된다)",
    "",
    "적용 방법 — 둘 중 하나",
    "  1) Firebase 콘솔에서 게시판을 한 번 열면 콘솔 오류에 색인 생성 링크가 뜬다. 클릭하면 끝.",
    "  2) firebase CLI 사용 시: firebase deploy --only firestore:indexes"
  ],
  "indexes": [
    {
      "collectionGroup": "board_posts",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "category", "order": "ASCENDING" },
        { "fieldPath": "createdAt", "order": "DESCENDING" }
      ]
    },
    {
      "collectionGroup": "board_posts",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "category", "order": "ASCENDING" },
        { "fieldPath": "likeCount", "order": "DESCENDING" }
      ]
    }
  ],
  "fieldOverrides": []
}
